eureka:
    client:
        serviceUrl:
            defaultZone: ${EUREKA_URL:http://user:password@localhost:8761}/eureka/
    instance:
        hostname: ${DOMAIN_NAME}
        nonSecurePort: 80
    password: ${USER_PASSWORD:password}
security:
    user:
        password: ${eureka.password}
spring:
    config:
        import: configserver:https://jrmj-config-server.herokuapp.com/
    datasource:
        password: n5vmur5r5cf2t0iv
    jackson:
        serialization:
            FAIL_ON_EMPTY_BEANS: false
    profiles: heroku
